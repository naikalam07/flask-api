If the test job fails, the build-docker job will NOT run because it depends on the test job using "needs: test".
This is important because it prevents broken or buggy code from being built into a Docker image or deployed.
It ensures only tested and working code moves forward in the pipeline, enhancing reliability.
